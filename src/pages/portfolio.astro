---
import BaseLayout from '../layouts/BaseLayout.astro';
import portfolioData from '../data/portfolio.json';
---

<BaseLayout 
    title="Portafolio | Edición Aular 🚀✨"
    description="Explora nuestros proyectos de branding, diseño web y ecommerce. Casos de éxito que transforman marcas. ¡Descubre nuestro trabajo! 🎨"
>
    <!-- Slider Container Wrapper -->
    <div class="bringer-slider-wrapper">
        <!-- Slider -->
        <div class="swiper bringer-slider" data-duration="800" data-effect="slide">
            <div class="swiper-wrapper">
                {portfolioData.map((project) => (
                    <div class="swiper-slide has-content">
                        <div class="bringer-slider-media">
                            <img class="bringer-lazy" src="/img/null.png" data-src={project.image} alt={project.title} width="1920" height="1280">
                        </div>
                        <div class="bringer-slide-content">
                            <div class="bringer-slide-content-inner bringer-slide-post-title">
                                <span class="bringer-meta">{project.category}</span>
                                <h4>{project.title}</h4>
                            </div>
                            <a href={`/portfolio/${project.slug}`}></a>
                        </div>
                    </div>
                ))}
            </div>
        </div>
        <!-- Slider Navigation -->
        <div class="bringer-slider-nav on-sides">
            <a href="#" class="bringer-slider-prev">
                <span class="bringer-icon bringer-icon-arrow-left"></span>
            </a>
            <a href="#" class="bringer-slider-next">
                <span class="bringer-icon bringer-icon-arrow-right"></span>
            </a>
        </div>
    </div>

    <style>
        /* PORTFOLIO FULLSCREEN - ESTRUCTURA EXACTA DEL ORIGINAL */
        
        /* Main ocupa TODA la pantalla (fixed) */
        body.bringer-fullscreen-page main#bringer-main {
            position: fixed;
            left: 0;
            right: 0;
            top: 0;
            width: 100%;
            height: 100svh;
            height: 100vh;
            padding: 0;
            margin: 0;
            z-index: 1;
        }
        
        body.bringer-fullscreen-page {
            overflow: hidden;
        }
        
        /* Slider wrapper ocupa todo el main */
        .bringer-slider-wrapper {
            width: 100%;
            height: 100%;
            position: relative;
        }
        
        /* Bringer slider con bordes redondeados */
        .bringer-slider {
            width: 100%;
            height: 100%;
            border-radius: 18px;
        }
        
        .swiper-wrapper {
            height: 100%;
        }
        
        .swiper-slide {
            height: 100%;
        }
        
        .bringer-slider-media {
            width: 100%;
            height: 100%;
            position: relative;
        }
        
        .bringer-slider-media img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
        }
        
        /* Header SOBRE el slider */
        body.bringer-fullscreen-page #bringer-header {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 100;
            background: rgba(7, 9, 13, 0.90);
            backdrop-filter: blur(10px);
        }
        
        body.bringer-fullscreen-page.no-header-border #bringer-header {
            border-bottom: none;
        }
        
        /* Footer OCULTO en portfolio fullscreen - MÚLTIPLES SELECTORES */
        body.bringer-fullscreen-page #bringer-footer,
        body.bringer-fullscreen-page footer,
        body.bringer-fullscreen-page .bringer-footer-widgets,
        body.bringer-fullscreen-page .bringer-footer-line,
        #bringer-footer,
        footer#bringer-footer {
            display: none !important;
            visibility: hidden !important;
            opacity: 0 !important;
            pointer-events: none !important;
            height: 0 !important;
            overflow: hidden !important;
            position: absolute !important;
            z-index: -9999 !important;
        }
        
        /* Navegación del slider */
        .bringer-slider-nav {
            z-index: 110;
        }
        
        /* Recuadro del nombre - MÁXIMA PRIORIDAD */
        .bringer-slide-content {
            position: absolute !important;
            z-index: 200 !important;
            bottom: 100px !important;
        }
        
        .bringer-slide-content-inner {
            background: rgba(0, 0, 0, 0.95);
            padding: 18px 28px;
            border-radius: 8px;
            backdrop-filter: blur(8px);
        }
        
        .bringer-slide-content-inner .bringer-meta {
            opacity: 0.8;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        
        .bringer-slide-content-inner h4 {
            margin: 5px 0 0 0;
            font-size: 1.8rem;
            font-weight: 600;
        }
        
        /* Responsive */
        @media (max-width: 1024px) {
            .bringer-slide-content {
                bottom: 90px !important;
            }
        }
        
        @media (max-width: 768px) {
            .bringer-slider-media img {
                object-fit: contain;
            }
            
            .bringer-slide-content {
                bottom: 80px !important;
            }
            
            .bringer-slide-content-inner {
                padding: 15px 22px;
            }
            
            .bringer-slide-content-inner h4 {
                font-size: 1.5rem;
            }
        }
        
        @media (max-width: 575px) {
            .bringer-slide-content {
                bottom: 70px !important;
                left: 15px !important;
                right: 15px !important;
            }
            
            .bringer-slide-content-inner {
                padding: 12px 18px;
            }
            
            .bringer-slide-content-inner h4 {
                font-size: 1.25rem;
            }
            
            .bringer-slide-content-inner .bringer-meta {
                font-size: 0.7rem;
            }
        }
    </style>

    <script is:inline>
        // CLASES EXACTAS DEL ORIGINAL + ELIMINAR FOOTER
        document.addEventListener('DOMContentLoaded', function() {
            // Agregar clases al body
            document.body.classList.add(
                'bringer-fullscreen-page',
                'no-header-border',
                'no-footer-border',
                'disable-smooth-scroll'
            );
            
            // Agregar clase al main
            const main = document.getElementById('bringer-main');
            if (main) {
                main.classList.add('is-fullscreen');
            }
            
            // ELIMINAR EL FOOTER DEL DOM COMPLETAMENTE
            const footer = document.getElementById('bringer-footer');
            if (footer) {
                footer.remove();
                console.log('✅ Footer eliminado del DOM');
            }
            
            // También eliminar footer-widgets si existe
            const footerWidgets = document.querySelector('.bringer-footer-widgets');
            if (footerWidgets) {
                footerWidgets.remove();
            }
            
            console.log('✅ Portfolio fullscreen: configuración completa');
        });
    </script>
</BaseLayout>
